<% @pics.each do |pic| %>
    <article class="tile ftg-set-1">
        <%= link_to '#' do  %>
            <%= image_tag(pic.photo.url, class: 'item') %>
            <div class="second-caption"><p>Test captionCaption</p></div>
        <% end %>  
    </article>
<% end %>
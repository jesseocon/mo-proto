<!DOCTYPE html>
<html>
    <head>
      <title>Folioshine | Home</title>
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <%= stylesheet_link_tag    "application", :media => "all" %>
      <%= javascript_include_tag "application" %>
      <%= csrf_meta_tags %>
      
      <!-- include Lightbox (optional) -->
      <%= stylesheet_link_tag "photoMosaic/files/includes/prettyPhoto/prettyPhoto.css" %>
      <%= javascript_include_tag "photoMosaic/files/includes/prettyPhoto/jquery.prettyPhoto.js" %>
      
      <!-- include PhotoMosaic -->
      <%= javascript_include_tag "photoMosaic/files/data/photoMosaic.data.js" %>
      <%= stylesheet_link_tag "photoMosaic/files/css/photoMosaic.css" %>
      <%= javascript_include_tag "photoMosaic/files/js/jquery.photoMosaic.js" %>
    </head>
    <body>
        <div id="container">
            <div class="row">
                <div class='span12'>
                    <%= link_to(
                            "Share",
                            "#myModal",
                            {
                                class: 'btn btn-primary',
                                data: {
                                    toggle: "modal"
                                }
                            }
                        ) 
                    %>
                </div>
            </div>
            <!-- Modal -->
            <div id="myModal" 
                class="modal hide fade" 
                tabindex="-1" 
                role="dialog"
                aria-labelledby="myModalLabel"
            >
                <div class="modal-header">
                    <button type="button" 
                        class="close" 
                        data-dismiss="modal" 
                        aria-hidden="true"
                    >
                        x
                    </button>
                    <h3 id="myModalLabel">Modal Header</h3>
                </div><!-- end of modal-header -->

                <div class="modal-body">

                </div><!-- end of modal-body -->

                <div class="modal-footer">
                    <button class="btn" data-dismiss="modal" aria-hidden="true">
                        Close
                    </button>
                    <button button class="btn btn-primary">
                        Save changes
                    </button>
                </div><!-- end of modal-footer-->
            </div><!-- end of shareModal-->
            <!-- End Modal -->
        </div> 
    </body>
</html>